$(document).ready(function () {
    $('.slider').slick({
        arrows: false,
        dots: true,
        speed: 1000,
        touchTreshold: 10,
        centerMode: true,
        fade: true,
        cssEase: 'linear',
    });
});
